import { ofType, ActionObservable } from 'redux-observable';
import { Observable } from 'rxjs/Observable';
import { getType } from 'typesafe-actions';

 import { } from './actions';
 import { ContainerActions } from './types';

// Individual exports for testing
export const {{ camelCase name }}: Epic<ContainerActions> = (
  action$,
  state$,
  {api},
  ) => {
  // See example in containers/HomePage/epic.ts
}
